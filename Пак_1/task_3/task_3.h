#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <math.h>

int q_function(
    const double eps,
    const double a,
    const double b,
    const double c
);

int m_function(const long long first, const long long second);

int t_tunction(
    const double eps,
    const double a,
    const double b,
    const double c
);
